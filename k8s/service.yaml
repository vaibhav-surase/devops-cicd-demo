apiVersion: v1
kind: Service
metadata:
  name: devops-cicd-demo-service
spec:
  type: LoadBalancer
  selector:
    app: devops-cicd-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
